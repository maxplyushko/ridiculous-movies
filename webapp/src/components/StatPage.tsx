import {PageLoader} from "./PageLoader.tsx";

const StatPage = () => {
  while (true) {
    return <PageLoader/>
  }
  return (
      <section className="stat-page">

      </section>
  );
};

export default StatPage;
